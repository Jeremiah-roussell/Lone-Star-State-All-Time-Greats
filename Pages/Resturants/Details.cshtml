@page
@model Final_Project.Pages.Resurants.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Resturant</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Resturant.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Resturant.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Resturant.Genre)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Resturant.Genre)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Resturant.Rating)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Resturant.Rating)
        </dd>
        <ul>
            @foreach (var c in Model.Resturant.ReviewResturants){
                <form method="post">
                        <input type="hidden" asp-for="DeleteReview" value="@c.FoodReviewer.FoodReviewerID" />
                        <li>
                            @c.FoodReviewer.Name <input type="submit" Value="Remove" class="btn btn-danger btn-sm my-1" asp-page-handler="DeletedReview" asp-route-id="@Model.Resturant.ResturantID">
                        </li>
                    </form>
            }
            {
                
            }
        </ul>
          <div class="row">
    <div class="col-md-4">
        <form method="post">
            <input type="hidden" asp-for="Resturant.ResturantID" />
            <div class="form-group">
                <select asp-items="Model.ReviewDropdown" asp-for="ReviewDropdown" class="form-control">
                    <option value="">--Select--</option>
                </select>
                <span asp-validation-for="AddReviewer" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Add Reviewer" class="btn btn-primary" asp-page-handler="" asp-route-id="@Model.Resturant.ResturantID"/>
            </div>
        </form>
    </div>
    </div>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Resturant.ResturantID">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
